apiVersion: apps/v1
kind: Deployment
metadata:
  name: ledger
  labels: { app: ledger }
spec:
  replicas: 1
  selector:
    matchLabels: { app: ledger }
  template:
    metadata:
      labels: { app: ledger }
    spec:
      initContainers:
        - name: wait-for-mysql
          image: busybox:1.36
          command: ["sh", "-c", "until nc -z mysql 3306; do echo waiting-mysql; sleep 2; done"]
      containers:
        - name: ledger
          image: infra-pointbank-ledger-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8084
          env:
            - name: LEDGER_DB_URL
              value: jdbc:mysql://mysql:3306/ledger_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
            - name: DB_USERNAME
              valueFrom: { secretKeyRef: { name: pointbank-secret, key: DB_USERNAME } }
            - name: DB_PASSWORD
              valueFrom: { secretKeyRef: { name: pointbank-secret, key: DB_PASSWORD } }
            - name: SQS_ENDPOINT
              value: http://localstack:4566
            - name: AWS_REGION
              value: ap-northeast-2
            - name: AWS_ACCESS_KEY_ID
              value: test
            - name: AWS_SECRET_ACCESS_KEY
              value: test
            - name: LEDGER_COMMAND_QUEUE_URL
              value: http://localstack:4566/000000000000/ledger-command-queue.fifo
            - name: LEDGER_COMMAND_DLQ_URL
              value: http://localstack:4566/000000000000/ledger-command-dlq.fifo
            - name: SECURITIES_RESULT_QUEUE_URL
              value: http://localstack:4566/000000000000/securities-result-queue.fifo
            - name: SECURITIES_RESULT_DLQ_URL
              value: http://localstack:4566/000000000000/securities-result-dlq.fifo
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8084 }
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 30
          livenessProbe:
            httpGet: { path: /actuator/health, port: 8084 }
            initialDelaySeconds: 40
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: ledger
  labels: { app: ledger }
spec:
  selector: { app: ledger }
  ports:
    - port: 8084
      targetPort: 8084
