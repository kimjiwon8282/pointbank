apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 환경 무관한 앱 5종. MySQL/LocalStack/Secret/NodePort는 overlay에서 주입.
namespace: pointbank

resources:
  - namespace.yaml
  - auth.yaml
  - ledger.yaml
  - securities.yaml
  - banking.yaml
  - gateway.yaml
