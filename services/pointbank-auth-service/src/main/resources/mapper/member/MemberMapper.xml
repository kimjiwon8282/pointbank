<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pointbank.auth.member.mapper.MemberMapper">

    <resultMap id="memberResultMap" type="com.pointbank.auth.member.domain.Member">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="phoneNumber" column="phone_number"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="role" column="role" javaType="com.pointbank.auth.member.domain.MemberRole"/>
        <result property="status" column="status" javaType="com.pointbank.auth.member.domain.MemberStatus"/>
        <result property="simplePasswordSet" column="simple_password_set"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="memberColumns">
        id, name, phone_number, password_hash, role, status,
        simple_password_set, created_at, updated_at
    </sql>

    <select id="findById" resultMap="memberResultMap">
        SELECT <include refid="memberColumns"/>
        FROM members
        WHERE id = #{id}
    </select>

    <select id="findByPhoneNumber" resultMap="memberResultMap">
        SELECT <include refid="memberColumns"/>
        FROM members
        WHERE phone_number = #{phoneNumber}
    </select>

    <select id="existsByPhoneNumber" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM members
        WHERE phone_number = #{phoneNumber}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO members (
            name, phone_number, password_hash, role, status, simple_password_set
        ) VALUES (
            #{name}, #{phoneNumber}, #{passwordHash}, #{role}, #{status}, #{simplePasswordSet}
        )
    </insert>

    <update id="updateSimplePasswordSet">
        UPDATE members
        SET simple_password_set = #{simplePasswordSet}
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE members
        SET status = #{status}
        WHERE id = #{id}
    </update>
</mapper>
