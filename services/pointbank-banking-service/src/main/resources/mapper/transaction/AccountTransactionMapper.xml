<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pointbank.banking.transaction.mapper.AccountTransactionMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO account_transactions (
            account_id, member_id, transfer_id, transaction_type,
            amount, balance_after, description
        ) VALUES (
            #{accountId}, #{memberId}, #{transferId}, #{transactionType},
            #{amount}, #{balanceAfter}, #{description}
        )
    </insert>
</mapper>
