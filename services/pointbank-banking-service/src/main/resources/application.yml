server:
  port: 8082

spring:
  application:
    name: pointbank-banking-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${BANKING_DB_URL:jdbc:mysql://localhost:3306/banking_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8}
    username: ${DB_USERNAME:pointbank}
    password: ${DB_PASSWORD:pointbank}

mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
  type-aliases-package: com.pointbank.banking
  configuration:
    map-underscore-to-camel-case: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
