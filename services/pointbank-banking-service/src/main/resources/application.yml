server:
  port: 8082

spring:
  application:
    name: pointbank-banking-service

management:
  endpoints:
    web:
      exposure:
        include: health,info

pointbank:
  ledger:
    base-url: ${LEDGER_SERVICE_BASE_URL:http://localhost:8084}
